<div className="mb-6">
  <h4 className="text-sm font-medium mb-2">{{ VARIANT_NAME_CAPITALIZED }}</h4>
  <div className="flex flex-wrap gap-4 mb-2">
    <Button variant="{{VARIANT_NAME}}" size="sm">
      Small
    </Button>
    <Button variant="{{VARIANT_NAME}}" size="md">
      Medium
    </Button>
    <Button variant="{{VARIANT_NAME}}" size="lg">
      Large
    </Button>
    <Button variant="{{VARIANT_NAME}}" disabled>
      Disabled
    </Button>
  </div>
  <p className="text-sm text-gray-500">{{ DESCRIPTION }}</p>
</div>
